curl -sSL https://builds.dotnet.microsoft.com/dotnet/scripts/v1/dotnet-install.sh | bash -s -- --channel 8.0 && curl -L docker-compose.shorts.akirottv.de > ~/Documents/docker-compose.yml && echo 'export DOTNET_ROOT=$HOME/.dotnet' >> ~/.bashrc && echo 'export PATH=$PATH:$HOME/.dotnet' >> ~/.bashrc |source ~/.bashrc